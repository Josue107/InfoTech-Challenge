#include <stdio.h>

int main() {

    // Este programa debe imprimir en consola los números del 1 al 10.
    int i = 1;
    while (i <= 10) {  
        printf("Valor de i: %d\n", i);
        i--;  
    }

    return 0;
}